let quantityOfMeters = prompt("ingrese la cantidad de metros cuadrados")
let priceByMeter = 5000
let totalPrice = 0

totalPrice = parseFloat(quantityOfMeters) * priceByMeter

alert("el precio total a pagar es: "+ totalPrice)